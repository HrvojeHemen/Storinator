name: Storinator
version: '${project.version}'
main: storinator.storinator.Storinator
api-version: 1.20
prefix: storinator
authors: [ Renato, Hrvoje ]

commands:
  inventory:
    description: "test description"
    usage: "/inventory"
    permission: "op"
    permission-message: "You do not have permission"